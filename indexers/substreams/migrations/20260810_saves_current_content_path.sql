-- Superseded by 20260810_social_current_tables.sql.
-- saves_current is now a sink-maintained table with bare content_path.
-- Kept as a no-op so already-applied deployments remain ordered.
SELECT 1;
