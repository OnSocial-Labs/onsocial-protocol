{
  "name": "onsocial-subgraph-sps",
  "version": "0.3.0",
  "description": "OnSocial Substreams-powered Subgraph - 100x faster indexing",
  "scripts": {
    "build": "graph build",
    "deploy:studio": "graph deploy --studio onsocial-testnet-sps",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 onsocial-testnet-sps"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1"
  }
}
