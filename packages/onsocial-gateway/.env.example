# Package-specific config (inherits from root .env)
# Copy to .env and customize

# Server
PORT=4000
NODE_ENV=development

# JWT (generate unique per environment!)
JWT_SECRET=dev-secret-change-in-production

# NEAR Contracts
# SOCIAL_TOKEN_CONTRACT=token.onsocial.testnet
# STAKING_CONTRACT=staking.onsocial.testnet

# The following are loaded from root .env:
# - HASURA_URL
# - HASURA_ADMIN_SECRET
# - LIGHTHOUSE_API_KEY
# - NEAR_NETWORK (optional)
# - NEAR_RPC_URL (optional)
