import { MapMarkerIcon } from '@onsocial/ui';

/** Quiet pin beside a location line — same glyph on portfolio and in the editor. */
export function PortfolioLocationMark() {
  return <MapMarkerIcon className="portfolio-location-mark" aria-hidden />;
}
