import { BuildingTreeIcon } from '@onsocial/ui';

/** Quiet office mark beside the org kind line — same size as the location pin. */
export function PortfolioOrgKindMark() {
  return <BuildingTreeIcon className="portfolio-location-mark" aria-hidden />;
}
