'use client';

/**
 * @deprecated Import `GestureSheetHeader` from `@onsocial/ui` instead.
 * Shim kept for existing app imports.
 */
export {
  GestureSheetHeader,
  type GestureSheetHeaderProps,
  type GestureSheetSignal,
} from '@onsocial/ui';
