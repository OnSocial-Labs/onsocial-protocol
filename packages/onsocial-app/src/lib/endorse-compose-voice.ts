/** Logged-out Endorse CTA — never Connect wallet. */
export const ENDORSE_CONNECT_CTA = 'Connect';

/** Endorse compose body — footer owns Connect. */
export const ENDORSE_CONNECT_HINT = 'Connect to put your name behind them.';

/** Painted / connected Endorse CTA — never Connect wallet. */
export const ENDORSE_SUBMIT_CTA = 'Endorse';

/** Endorse compose body when a wallet is painted. */
export const ENDORSE_CONNECTED_HINT =
  'Public vouch — topic, note, and media are optional.';
