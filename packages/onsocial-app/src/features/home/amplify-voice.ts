/** Logged-out Amplify CTA — never Connect wallet. */
export const AMPLIFY_CONNECT_CTA = 'Connect';

/** Amplify sheet body — footer owns Connect. */
export const AMPLIFY_CONNECT_HINT = 'Connect to amplify with SOCIAL.';
