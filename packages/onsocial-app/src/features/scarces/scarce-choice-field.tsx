'use client';

/**
 * Scarce list choice chips — thin alias over the shared choice drawer so
 * feature imports stay stable.
 */
export {
  ChoiceDrawerField as ScarceChoiceField,
  type ChoiceOption as ScarceChoiceOption,
} from '@/components/ui/choice-drawer';
