/** Logged-out Message CTA — never Connect wallet. */
export const DM_CONNECT_CTA = 'Connect';

/** Message compose body — footer owns Connect. */
export const DM_CONNECT_HINT = 'Connect to message them.';
