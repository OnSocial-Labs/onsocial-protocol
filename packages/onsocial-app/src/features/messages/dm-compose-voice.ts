/** Logged-out Message CTA — never Connect wallet. */
export const DM_CONNECT_CTA = 'Connect';

/** Message compose body — the sheet already names the person. Footer owns Connect. */
export const DM_CONNECT_HINT = 'Connect to message.';
