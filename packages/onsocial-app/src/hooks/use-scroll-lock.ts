/**
 * @deprecated Import `useScrollLock` from `@onsocial/ui` instead.
 * Shim kept for existing app imports during migration.
 */
export {
  configureScrollLockContainerFinder,
  useScrollLock,
  type ScrollLockContainerFinder,
} from '@onsocial/ui';
