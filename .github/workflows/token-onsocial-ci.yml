name: Token CI

on:
  push:
    branches: [main]
    paths:
      - 'contracts/token-onsocial/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/token-onsocial-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'contracts/token-onsocial/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/token-onsocial-ci.yml'
  workflow_dispatch:

jobs:
  ci:
    name: Check, Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Check
        run: make check-contract-token-onsocial

      - name: Lint
        run: make lint-contract-token-onsocial

      - name: Format
        run: make format-contract-token-onsocial

      - name: Build
        run: make build-contract-token-onsocial

      - name: Test
        run: make test-unit-contract-token-onsocial

      - name: Fix artifact permissions
        run: sudo chown -R $(id -u):$(id -g) target/near/

      - name: Upload WASM
        uses: actions/upload-artifact@v4
        with:
          name: token-onsocial.wasm
          path: target/near/token_onsocial/token_onsocial.wasm
          retention-days: 7
