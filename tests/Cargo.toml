[package]
name = "onsocial-integration-tests"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/mod.rs"

[dependencies]
near-workspaces = "0.18.0"
near-crypto = "0.30.1"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1.35", features = ["macros", "rt-multi-thread"] }
anyhow = "1.0"
auth-onsocial = { path = "../contracts/auth-onsocial" }
ft-wrapper-onsocial = { path = "../contracts/ft-wrapper-onsocial" }
relayer-onsocial = { path = "../contracts/relayer-onsocial" }