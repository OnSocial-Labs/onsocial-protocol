[package]
name = "onsocial-types"
version = "0.1.0"
edition = "2024"
description = "Shared types and pure-logic utilities for the OnSocial protocol"

[dependencies]
serde_json = { version = "1.0", features = ["preserve_order"] }
